const velocidade = 200;
const warning = 85;

// condição ? verdadeiro : false
const condicao = velocidade >= warning ? console.log("Recebeu uma multa") : console.log("Continua andando"); 

// if(velocidade >= warning) {
//     console.log("Recebeu uma multa");
// }